import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";

export const ProductsOrdersContainer = styled(Box)(({ theme }) => ({
  //styles
}));

export const OrdersContainer = styled(Box)(({ theme }) => ({
  //styles
}));
